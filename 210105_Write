메소드 - 소스구조를 모듈화(재사용, 간결성)

1. void func( ) {
  실행문;
    }
2. void func(매개변수) {
  실행문;
  }
  
  매개변수 -> 메소드를 사용하는 곳에서 전달하는 정보, 여러개 가능
  
3. 타입 func ( ) {  //타입과 리턴값이 같아야한다. 예를 들면 타입이 정수값(int)이면 리턴 값도 정수값(int)으로
  실행문;
  return값;
  }
4. 타입 func (매개변수) {
  실행문;
  return값;
  }
