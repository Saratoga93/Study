메소드 (모듈화)

1. void func ( ) {
    실행문;
  }
2. void func (int a, char ch{매개변수: 1.다양한 타입, 2. 개수제한 없음}) {
    실행문;
  }
3. 타입 func ( ) {
    실행문;
    반환값; (리턴값)
  }

4. 타입 func(매개변수) {
    실행문;
    반환값;
  }
