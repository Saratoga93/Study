//Math.abs() : 절대값 표현

반복문(Loop문)
1. for(초기값 조건; 증가(감소)) {
  실행문;
 }
 
 초기값 = 0;
 2. While(조건) {
  실행문;
  초기값++;
 }
 
 3. do {
  실행문;
  초기값 증가(감소);
} while(조건);

4. 무한루프 
While(true) [= for( ; ; ) {
  실행문;
  break; -> 루프 탈출
}

5. 이중루프



컨트롤키 + / --> 선택한 전체 줄 주석화
